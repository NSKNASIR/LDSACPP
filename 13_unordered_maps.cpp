#include <bits/stdc++.h>
using namespace std;
int main(){
    //all are same without time complexity and valid keys.
    unordered_map<int,string> m; // unordered_map<pair<int,int>,string> m; not use.
    m[3]="abc";//O(1); 

    //multimap
    multimap<int,vector<int>> ml;//multipule value exit.
}